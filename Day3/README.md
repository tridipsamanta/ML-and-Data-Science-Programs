# 📘 Dictionary Word Meaning App (Python)

This is a simple Python program that allows users to enter **any English word** and get its **meaning and part of speech** using a **free Dictionary API**.

The project is beginner-friendly and does **not require any API key**.

---

## 🚀 Features
- Takes any word as user input
- Fetches real dictionary definitions
- Shows:
  - Part of speech (noun, verb, adjective, etc.)
  - Meaning of the word
- Uses a **100% free public API**
- Simple and easy-to-understand code

---

## 🛠️ Technologies Used
- Python 3
- `requests` library
- Free Dictionary API (`dictionaryapi.dev`)

---

## 📦 Requirements
Make sure Python is installed, then install the required library:

```bash
pip install requests
